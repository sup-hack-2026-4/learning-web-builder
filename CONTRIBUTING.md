# 共同開発ガイド

## ブランチ

`main`へ直接pushしません。Issueごとに次の形式でブランチを作成します。

- `feat/123-short-name`
- `fix/123-short-name`
- `docs/123-short-name`

## 開発フロー

1. Issueで目的と完了条件を共有する
2. 最新の`develop`から作業ブランチを作る
3. 小さな単位でコミットする
4. ローカルでテストを実行する
5. Draft PRを作り、実装中の判断も説明する
6. CI成功とCODEOWNERSレビュー後に管理者がマージする

## コミット例

- `feat: サイトプレビューを追加`
- `fix: 空のaltを品質チェックで検出`
- `docs: ローカル起動手順を更新`

## PRの最小条件

- 対応Issueを記載
- 変更内容と確認方法を記載
- UI変更はスクリーンショットを添付
- 新しい環境変数は`.env.example`にも追加
- 仕様変更は`docs/`または`openapi/`も更新

秘密情報、個人情報、生成した提出物はコミットしないでください。
