version: "2"
sql:
  - engine: "postgresql"
    queries: "queries"
    schema: "migrations"
    gen:
      go:
        package: "dbgen"
        out: "../backend/internal/dbgen"
        sql_package: "pgx/v5"
        emit_json_tags: true

